#include<stdio.h>
#include<stdlib.h>
int main()
   {
	int mais[5],cont=0,soma;
	int i,andares,entrar,sair;
	printf("Quantos andares tem o predio:");
	scanf("%d",&andares);
	//entraram
	printf("\n");
	for(i=0;i<andares;i++){
		if(cont<16)
		{
		printf("%d andar \n",i+1);
		printf("quantas pessoas entraram:");
		scanf("%d",&entrar);
		cont=cont+entrar;
		printf("quantas pessoas sairam:");
		scanf("%d",&sair);
		cont=cont-sair;
		printf("Tem %d pessoas no elevador\n\n", cont);
	                      }
         }
	if(cont>15)
	{
		printf ("Excesso de Pessoas\n");
			if (cont >= 15){
				soma=cont-15;
				printf ("Devem sair %d pessoas\nQuantas pessoas irao sair: ",soma);	
	                     scanf ("%d", &sair);
			            printf ("\n");
			            cont = cont - sair;
					}
	printf ("Chegamos ao ultimo andar.\n\nSairao %d pessoas", cont);
	  }
		
		

	system("pause") ;
	return 0;	
	}
